# Railway Railpack Configuration
# https://railpack.com/languages/java

[build]
builder = "RAILPACK"

[deploy]
startCommand = "java -Dspring.profiles.active=production -Dserver.port=$PORT -jar build/libs/BilliBRESTJava-*.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
